{
  "scripts": {
    "start": "bundle exec jekyll serve",
    "build": "bundle exec jekyll build"
  },
  "requires": true,
  "lockfileVersion": 1,
  "dependencies": {
    "fuzzysearch": "1.0.3",
    "simple-jekyll-search": "^1.9.2"
  }
}
